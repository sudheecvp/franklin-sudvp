version: 1
indices:
  enrichment:
    target: /enrichment/enrichment.json
    include:
      - '**/magazine/**'
    properties:
      title:
        select: head > meta[property="og:title"]
        value: |
          attribute(el, 'content')